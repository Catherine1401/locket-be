t cần implement function kết bạn

đầu tiên, t cần viết endpoint cho phép users get 1 profile của ai đó

sau đó, t cần viết 1 endpoint cho phép users gửi lời mời kết bạn, data sẽ được
lưu vào table friends_request

vấn đề là, trước khi gửi lời mời kết bạn, t cần biết họ có đang phải bạn bè hay
không?

để biết được điều này, t cần query table friends để kiểm tra bạn bè của user gửi
request

t cần 1 cái gì đó để lấy ra tất cả bạn bè cùa 1 users

tiếp đến, t cần viết endpoint cho phép users xem danh sách yêu cầu kết bạn

tiếp nữa, t cần viết endpoint cho phép usere phản hồi (accept or reject) lời mời
data sẽ được lưu vào table friends

===================

hiện tại t đã lấy được thông tin users khác thông qua shareCode, t cũng đã trả
về status_friend và xử lý trường hợp trả về request là chính mình

 t cần implement function request friend

đầu tiên, nếu có 1 yêu cầu kết bạn mới, t cần thêm 1 record vào table
request_friends

nếu chấp nhận, t cần thêm 1 record vào table friends

nếu từ chối, t cần chuyển trạng thái của trường status sang reject

vậy nếu sau khi bị reject thì có gửi lại lời mời được không?

câu trả lời là có. khi gửi lại lời mời, không phải ghi thêm một bản ghi mới,
mà check xem có bản ghi nào chưa, nếu có và ở status reject, cần chuyển sang
penđing, nếu chưa có, cần ghi một bản ghi mới

nên dể trong route users hay tách ra route friends?
nếu để trong route users thì sao?

chả sao cả, chỉ là code sẽ dài hơn trong một file thôi

nếu tách ra, file sẽ ngắn và clear chức năng hơn. thôi từ dầu mình tách rồi thì
giờ tách luôn đi

==============

ok, vậy là t đâ xong function create friend request

giờ t cần implement function xem danh sách yêu cầu kết bạn

t cần chọc vào cái table request_friends để lấy ra tất cả các record có id của t
là trường to_user_id where status = pending

đã thao tác với db xong, tiếp t cần viết controller để control route

có cần middleware gì không nhở

ok, t đã xong function xem danh sách yêu cầu kết bạn

tiếp t cần reponse lại

t cần update info user.

user có rất nhiều trường thông tin, nếu update từng cái một thì đến mùa quýt năm
sau. chưa kể, truy vấn vào database quá nhiều lần làm giảm hiệu năng. t chỉ muốn
query một lần duy nhất.

vấn đề là phải bóc tách được cần update những field nào.

dùng vòng for of cho object fields truyền vào có thể bóc được những field nào
cần update.

nhưng query thì không thể biến hóa động theo field được. chẳng lẽ dùng 100 cái
if else?

nếu update từng field một thì chẳng khác gì option 1 như đã đề cập.

t cần query động theo fields

========================

t đã làm get/update user profile
t đã create moment sử dụng cloudinary để lưu ảnh

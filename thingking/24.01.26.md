t cần thêm 1 function là upload thumbnail lên cloudinary

đầu tiên, t cần thêm 1 cột thumbnail trong vào table moments

quy trình thực hiện của t sẽ như nào?

1. route t vẫn giữ nguyên
2. t cần một controller để control route này. this sẽ có trách nhiệm nhận data
sạch, gọi hàm tương tác với database và cloudinary, trả về info sucess
3. liệu cần middleware ở đây không? trước hết t cần một middleware để check
input
4. t cần một function ghi data vào database, 1 fucntion ghi ảnh lên cloudinary

tiếp theo t cần clear lại luồng kết bạn một chút về flow friends

**để gửi yêu cầu được cho 1 users, t cần sharecode của user đó. dựa vào sharecode
t cần 1 api trả về thông tin user bao gồm id, name, avatar, status
(friend, unfriend, incoming, outgoing)**

để biết được status, t lại phải truy vấn bảng request_friends và bảng friends

==> t cần thao tác với với api liên quan đến request_friends và friends trước
khi create api getUserByShareCode

khi tạo một yêu cầu kết bạn, t cần ghi một record mới vào request_friends.
record phải là unique với status = 'pending'. nếu đã có record với status
= 'rejected', t cần update record với status = 'pending'

vậy luông như nào?

- t cần 1 endpoint hứng api này
- t cần 1 controller để control. controller này sẽ nhận userid hợp lệ, chọc vào
database, trả về message tương ứng với việc thành công hay thất bại
- t cần 1 middleware check xem userid có tồn tại hay không

t cần viết một api lấy danh sách yêu cầu kết bạn. t cần truy vấn bảng
request_friends và lấy ra tất cả các bản ghi có id của t và status = 'pending'
nên nhớ rằng, object trong trường hợp này là request_friend chứ không phải user

sau khi lấy được danh sách yêu cầu kết bạn, t cần viết một api cho phép user
response những người đó.

trong trường hợp user đồng ý, t cần chuyển status từ 'pending' sang 'accepted',
đồng thời ghi một record vào bảng friends với status = 'friend'.

trong trường hợp user từ chối, t cần chuyển status từ 'pending' sang 'rejected'

user sau khi bị từ chối hoàn toàn có quyền gửi lại yêu cầu kết bạn. khi gửi lại
yêu cầu, record đã có trong table request_friends, t cần chuyển status từ 
'rejected' sang 'pending' ==> không phải insert một record mới mà là upsert

user có quyền xem danh sách bạn bè bằng cách truy vấn bảng friends và lấy ra
record có chứa id của mình với status = 'friend'. nên nhớ rằng đối tượng thao
tác trong case này là friendship, not user.

user có quyền hủy kết bạn = cách chuyển status trong bảng friends từ 'friend'
sang 'unfriend'. đồng thời xóa record có chứa id 2 đứa trong bảng request_friends

vậy lật ngược lại một chút, khi đồng ý kết bạn. ghi thêm record nếu chưa có, 
sửa nếu status đang là 'unfriend' ==> 'friend', updated_at = now()

=============

đầu tiên, t cần viết api cho việc lấy user từ sharecode

t cần 1 route
t cần 1 controller. controller sẽ làm nhiệm vụ mang sharecode chọc vào database
để lấy ra data kèm status
t cần 1 hàm check xem có phải friend không
t cần 1 hàm check xem có phải người gửi không
t cần một hàm check xem có phải người nhận không
controller dựa vào 3 hàm này để trả về status
t cần 1 middleware check xem trong database thực sự có ai có sharecode này không

ok, vậy là t đã viết xong chức năng get user by sharecode
t sẽ commit như sau
git commit -m "refactor: "

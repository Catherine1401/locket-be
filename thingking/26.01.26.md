/moments/feed?limit=20&prevcursor=:id&nextcursor=:id

có 4 trường hợp có thể xảy ra

prevcursor = nextcursor = null. đây là trường hợp khi load app lần đầu. server cần query lấy 20 moment từ đầu và trả về kèm nextcursor + prevEnd = true + nextEnd = true | false.

prevcursor = null, nextcursor = ?. đây là trường hợp scroll dọc bị hết data và cần lấy thêm. server cần query lấy 20 moments từ nextcursor và trả về nextcursor + nextEnd = true | false.

prevcursor = ?, nextcursor = null. đây là trường hợp scroll lên bị hết data và cân lấy thêm, server cần query 20 moments từ prevcursor và trả về prevcursor = ? + prevEnd = true | false.

prevcursor = nextcursor = ?. đây là trường hợp từ grid nhảy vào feed và không có sẵn data. server cần query 40 feed quay cursor, trả về prevcursor = ? + nextcursor = ? + prevEnd = true | false + nextEnd = true | false.

th1

- t cần lấy limit
- t đi vào trường hợp đầu tiên. t cần 1 model chọc vào db lấy 20 moments của bạn
  bè và user đó => t cần lấy thêm userId
  => t cần 1 hàm lấy danh sách friendIds của user đó
  đã có friendIds rồi làm sao t check được 1 moments có userId hợp lệ hay không
  t sẽ sử dụng toán tử IN trong sql

th2

t cần 1 model chọc vào db lấy 20 moments tính từ nextcursor
